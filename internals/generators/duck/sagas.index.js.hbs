// import { takeEvery } from 'redux-saga/effects';
// import trackRequests from 'rdx/utils/trackRequests';

import types from 'rdx/modules/{{ camelCase name }}/types';
// import get{{ pascalCase name }} from 'rdx/modules/{{ camelCase name }}/sagas/get{{ pascalCase name }}';

function* watch{{ pascalCase name }}Sagas() {
  // yield trackRequests(takeEvery, types.REQUEST_GET_EXAMPLE, get{{ pascalCase name }});
}

export default watch{{ pascalCase name }}Sagas;
